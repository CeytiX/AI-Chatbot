using namespace std;

class Bot
{
	private:
		Bot();
		Bot(string);
		~Bot();
		void talk();
		string answerTo(string);
		void newAnswer(string);

	public:
		map <string, map <string, int> > memory;
		string name;
		string answer;
		bool firstTime;
};
